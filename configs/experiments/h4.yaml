# H4 — Summary-level inference disclosure.
# plan-v3 §6: held-out reader recovers protected facts, compression at 4x reduces disclosure.
hypothesis: h4
benchmark_path: data/processed/c1-v0.1
compressors: ["lingua2", "phi3-extractive", "filter"]
ratios: [4.0]
seeds: [0]
workload_families: ["a", "b", "c"]
model_size: "8b"
backend: "ollama"
out_dir: "results"
include_no_compression_control: false
require_trained_compressors: false
notes: "plan-v3 H4; priors vs baseline vs compressed_4x disclosure."
